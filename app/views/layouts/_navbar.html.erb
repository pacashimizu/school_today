<nav class="navbar navbar-expand-md navbar-dark bg-dark sticky-top mb-1">
  <div class="container">
    <!-- ブランド -->
    <a class="navbar-brand" href="/">さくら保育園 Daily News</a>
    <!-- 切り替えボタン -->
    <% if logged_in? %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- ナビゲーション -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <!-- 教員のナビゲーション -->
          <% if current_user.teacher? %>
            <ul class="navbar-nav ms-auto gap-1">
              <li class="nav-item"><%= link_to "新しい記事を書く", new_article_path, class: "nav-link btn btn-primary text-light" %></li>
              <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle btn btn-outline-secondary text-light" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <%= current_user.name %> さん  
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to "園児一覧", users_path, class: "dropdown-item" %>
                  <%= link_to "教員一覧", teachers_users_path, class: "dropdown-item" %>
                  <%= link_to "クラス一覧", grades_path, class: "dropdown-item" %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "ユーザの新規登録", signup_path, class: "dropdown-item" %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "ログアウト", logout_path, method: :delete, class: "dropdown-item" %>
                </div>
              </li>
            </ul>
          <!-- 園児のナビゲーション -->
          <% else %>
            <ul class="navbar-nav ms-auto">
              <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle btn btn-outline-secondary text-light" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <%= current_user.name %> さん  
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to "教員一覧", teachers_users_path, class: "dropdown-item" %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "ログアウト", logout_path, method: :delete, class: "dropdown-item" %>
                </div>
              </li>
            </ul>
          <% end %>
      </div>
    <% end %>
  </div>
</nav>